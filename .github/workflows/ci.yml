name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: CI placeholder
        run: echo "CI runs unit tests and linters. Configure Gradle to run real tasks."
      - name: Lint placeholder
        run: echo "Run ktlint/detekt here if configured"
